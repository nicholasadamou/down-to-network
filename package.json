{
	"name": "downtonetwork",
	"version": "1.0.0",
	"description": "A dynamic web / mobile app optimized to connect new fellow IBMers to experienced IBMers through the power of matching.",
	"scripts": {
		"install-dependencies": "cd backend/generated && yarn install && cd ../../frontend/ && yarn install",
		"build": "cd frontend/ && yarn build",
		"clean": "cd backend/ && yarn clean && cd ../frontend/ && yarn clean"
	},
	"license": "MIT",
	"repository": {
		"url": "https://github.com/nicholasadamou/DownToNetwork"
	}
}
